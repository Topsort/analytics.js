name: Deploy Playground to GitHub Pages

on:
  push:
    branches:
      - main
    paths:
      - "demo/**"
      - "src/**"
      - "package.json"
      - "vite.config.*"
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  deploy:
    runs-on: ubuntu-22.04
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - name: Checkout
        uses: actions/checkout@v7

      - uses: pnpm/action-setup@v6
        name: Install PNPM

      - name: Set up Node.js
        uses: actions/setup-node@v6
        with:
          cache: pnpm
          node-version-file: package.json
          registry-url: https://registry.npmjs.org

      - name: Install dependencies
        run: pnpm install

      - name: Build
        run: pnpm run build

      - name: Prepare pages artifact
        run: |
          mkdir -p _site/dist
          cp demo/index.html _site/index.html
          cp dist/ts.iife.js _site/dist/ts.iife.js

      # Artifact names are scoped to the workflow run, not the attempt. Re-running
      # failed jobs uploads a second "github-pages" artifact and deploy-pages@v5
      # aborts. Suffix with run_attempt so each attempt is unique.
      - name: Upload artifact
        uses: actions/upload-pages-artifact@v5
        with:
          name: github-pages-${{ github.run_attempt }}
          path: _site

      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v5
        with:
          artifact_name: github-pages-${{ github.run_attempt }}
